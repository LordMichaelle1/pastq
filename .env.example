# RevenueCat public SDK keys — safe to ship in the app binary.
# Get them from app.revenuecat.com → Project → API keys → Public app-specific keys.
# Copy this file to .env and fill in. Never put the secret key here.
EXPO_PUBLIC_RC_ANDROID_KEY=
EXPO_PUBLIC_RC_IOS_KEY=
